name: Debug Hetzner DNS

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Test Hetzner API
        env:
          HETZNER_TOKEN: "FxSS691YDR3ifdOKpc6DYn46jYs3UkEcQL4Rj9b5FjJwR91Lvrl19H1xw9MrEZhS"
        run: |
          echo "Test 1: Alle Zones"
          curl -s -H "Auth-API-Token: $HETZNER_TOKEN" https://dns.hetzner.com/api/v1/zones | head -200
          
          echo ""
          echo "Test 2: Zone ob5.dev"
          curl -s -H "Auth-API-Token: $HETZNER_TOKEN" "https://dns.hetzner.com/api/v1/zones?name=ob5.dev"
